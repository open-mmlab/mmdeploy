{
    "mmdet": {
        "branch": "3.x",
        "version": "mmdet>=3.0.0rc1",
        "mmcv": "mmcv>=2.0.0rc1",
    },
    "mmcls": {
        "branch": "3.x",
        "key": "value"
    }
}



// git clone --depth 1 --branch ${branch} --recursive https://github.com/open-mmlab/${codebase_fullname}.git
// for key, value in ${codebase} expect branch do
//      pip install ${value}
//

// example
// git clone --depth 1 --branch ${branch} --recursive https://github.com/open-mmlab/mmdetection.git
// pip install ${version}
// pip install ${mmcv}
